{% render 'hero' with {
    settings: section.settings
} %}


{% schema %}

    {
        "name": "Hero",
        "settings": [
            {
                "type": "text",
                "id": "heading",
                "label": "heading"
            }
        ],
        "presets": [
            {
                "name": "Hero",
                "category": "Image w/ text"
            }
        ]
    }

{% endschema %}